{
  "name": "music-class-app",
  "version": "1.0.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/pascalvaccaro/strapi-svelte.git"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "scripts": {
    "build:admin": "(cd admin && yarn build)",
    "build:app": "(cd app && npm run build)",
    "build": "concurrently \"npm run build:admin\" \"npm run build:app\"",
    "dev": "concurrently \"npm run dev:admin\" \"npm run dev:app\"",
    "dev:admin": "(cd admin && yarn develop)",
    "dev:app": "(cd app && yarn dev)"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
